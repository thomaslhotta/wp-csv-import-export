WordPress CSV Import-Export
===========================

A WordPress plugin that allows importing users, posts and comments from CSV files and exporting users.

User import/export supports BuddyPress xprofile fields and user meta. 
